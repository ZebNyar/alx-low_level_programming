#include "main.h"
#include <stdio.h>
/**
 * nul - multiplies 2 integers
 * @a: The first integer
 * @b: the second intager
 * Return: Always 0
 */

int mul(int a, int b)
{
	int result = a * b;
	return (result);
}
